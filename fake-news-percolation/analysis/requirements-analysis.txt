# Python deps for the EVIDENCE / EDA analysis scripts (build_evidence.py {eda|compare|influencer},
# ram_monitor.py and utils.py).
# These run in the `mzungu` conda env, NOT the `ABM` env used by sobol/ (see sobol/requirements.txt).
numpy
pandas
scipy
pyarrow
statsmodels   # mixed-effects model, lowess calibration, Benjamini-Yekutieli FDR
diptest        # Hartigan dip test of unimodality (replaces Sarle's bimodality coefficient)
psutil         # memory watchdog in ram_monitor.py (OOM guard for the heavy subcommands)
